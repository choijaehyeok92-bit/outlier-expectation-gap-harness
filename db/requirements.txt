# The database layer's own dependencies. The harness runtime stays on the
# standard library; nothing in harness_core imports this package.
SQLAlchemy>=2.0,<3
alembic>=1.13,<2
psycopg[binary]>=3.1,<4    # PostgreSQL driver; SQLite needs no driver
