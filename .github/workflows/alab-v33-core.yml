name: ALAB v3.3 core domain analysis

on:
  push:
    branches:
      - work/alab-2026-09-18-v33
    paths:
      - ".github/workflows/alab-v33-core.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  core:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: work/alab-2026-09-18-v33
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install -r requirements-dev.txt
      - name: Execute ALAB core domain plan
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p runlogs
          exec > >(tee runlogs/alab-core.log) 2>&1

          python harness.py selftest
          python harness.py intake ALAB
          python harness.py validate-pack ALAB

          for AGENT in CP MA MT RF SL; do
            python harness.py prompt ALAB "$AGENT" --out "runs/ALAB/${AGENT}_prompt.md"
            cp ".harness_inputs/ALAB/reports/${AGENT}.json" "runs/ALAB/reports/${AGENT}.json"
            python harness.py validate ALAB "$AGENT"
          done

          python harness.py aggregate ALAB | tee runlogs/aggregate_after_core.json
          python harness.py digest ALAB
          python harness.py plan ALAB | tee runlogs/plan_after_core.txt

          echo "===== DIGEST ====="
          cat runs/ALAB/digest.md
          echo "===== NEXT PLAN ====="
          cat runlogs/plan_after_core.txt

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add runs/ALAB
          if ! git diff --cached --quiet; then
            git commit -m "core: complete ALAB CP MA MT RF SL"
            git push origin HEAD:work/alab-2026-09-18-v33
          fi
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: ALAB-v33-core
          path: |
            runs/ALAB
            runlogs
