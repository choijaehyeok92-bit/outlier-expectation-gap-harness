name: META MO validate 2026-09-21

on:
  push:
    branches:
      - work/meta-2026-09-21-v33
    paths:
      - ".github/workflows/meta-mo-validate-20260921.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: work/meta-2026-09-21-v33
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install -r requirements-dev.txt
      - name: Validate and cache MO
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p runlogs
          exec > >(tee runlogs/meta-mo-validate.log) 2>&1
          python harness.py validate META MO
          python harness.py cache-macro META
          python harness.py aggregate META | tee runlogs/aggregate-after-mo.json
          python harness.py digest META
          python harness.py plan META | tee runlogs/plan-after-mo.txt
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add runs/META runlogs
          if ! git diff --cached --quiet; then
            git commit -m "run: validate META macro overlay 2026-09-21"
            git push origin HEAD:work/meta-2026-09-21-v33
          fi
